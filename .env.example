# IntelliDoc 환경 설정 파일
# 이 파일을 복사하여 .env 파일로 만들고 실제 값으로 수정하세요

# ======================
# 데이터베이스 설정
# ======================
POSTGRES_DB=intellidoc
POSTGRES_USER=intellidoc
POSTGRES_PASSWORD=intellidoc123
POSTGRES_PORT=5432

# ======================
# Redis 설정
# ======================
REDIS_PORT=6379

# ======================
# 애플리케이션 포트 설정
# ======================
BACKEND_PORT=8000
FRONTEND_PORT=3000
FLOWER_PORT=5555
NGINX_PORT=80
NGINX_SSL_PORT=443

# ======================
# OCR 엔진 설정
# ======================
# Tesseract 설정
TESSERACT_LANG=kor+eng

# Mistral OCR 설정
MISTRAL_API_KEY=your-mistral-api-key-here
MISTRAL_API_URL=https://api.mistral.ai/v1/chat/completions
MISTRAL_MODEL=pixtral-12b-2409

# Google Vision API 설정 (선택사항)
GOOGLE_CLOUD_PROJECT_ID=your-project-id
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# AWS Textract 설정 (선택사항)
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key
AWS_DEFAULT_REGION=us-east-1

# ======================
# 보안 설정
# ======================
SECRET_KEY=your-super-secret-key-here-please-change-this
JWT_SECRET_KEY=your-jwt-secret-key-here
ENCRYPTION_SALT=your-unique-encryption-salt-minimum-32-characters-long

# 허용할 호스트 (쉼표로 구분, 프로덕션에서는 명시적으로 지정)
# 예: your-domain.com,www.your-domain.com
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# CORS 허용 오리진 (쉼표로 구분, 프로덕션에서는 실제 도메인만 포함)
# 개발: http://localhost:3000,http://127.0.0.1:3000
# 프로덕션: https://your-domain.com
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ======================
# 로깅 설정
# ======================
LOG_LEVEL=INFO
LOG_FILE=/app/logs/intellidoc.log

# ======================
# 배치 처리 설정
# ======================
BATCH_MAX_WORKERS=4
BATCH_USE_PROCESSES=false
CELERY_WORKERS=4

# ======================
# 파일 업로드 설정
# ======================
MAX_FILE_SIZE=100MB
ALLOWED_FILE_TYPES=pdf,jpg,jpeg,png,tiff,tif,bmp,gif
UPLOAD_DIR=/app/uploads

# ======================
# 프론트엔드 설정
# ======================
REACT_APP_API_URL=http://localhost:8000
REACT_APP_VERSION=1.0.0
REACT_APP_MAX_FILE_SIZE=100
REACT_APP_SUPPORTED_FORMATS=PDF,JPG,JPEG,PNG,TIFF,BMP,GIF

# ======================
# 개발 모드 설정
# ======================
DEBUG=false
DEVELOPMENT_MODE=false
ENABLE_DEBUG_TOOLBAR=false

# ======================
# 데이터베이스 백업 설정
# ======================
BACKUP_ENABLED=true
BACKUP_SCHEDULE=0 2 * * *
BACKUP_RETENTION_DAYS=30
BACKUP_S3_BUCKET=intellidoc-backups

# ======================
# 모니터링 설정
# ======================
ENABLE_METRICS=true
PROMETHEUS_PORT=9090
GRAFANA_PORT=3001

# ======================
# 캐시 설정
# ======================
CACHE_TTL=3600
ENABLE_QUERY_CACHE=true
ENABLE_RESULT_CACHE=true
